define maks = Character(
    None,                   # no name
    color="#64AAD0",        # text color (when there is no name, color is applied to the text)
    what_color="#64AAD0"    # duplicate it just in case =/
)